
  import React from 'react';
  import { AlignVerticalJustifyEnd, LucideProps } from "lucide-react";

  export default function (props: LucideProps) {
    return <AlignVerticalJustifyEnd {...props} />;
  }
