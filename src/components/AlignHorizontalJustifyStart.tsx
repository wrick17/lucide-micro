
  import React from 'react';
  import { AlignHorizontalJustifyStart, LucideProps } from "lucide-react";

  export default function (props: LucideProps) {
    return <AlignHorizontalJustifyStart {...props} />;
  }
