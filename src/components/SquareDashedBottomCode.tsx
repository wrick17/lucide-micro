
  import React from 'react';
  import { SquareDashedBottomCode, LucideProps } from "lucide-react";

  export default function (props: LucideProps) {
    return <SquareDashedBottomCode {...props} />;
  }
